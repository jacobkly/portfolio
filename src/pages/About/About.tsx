import Header from '../../components/Header/Header';
import './about.css';

const About: React.FC = () => {
    return (
        <div id="about-container">
            <Header section="About" left={34} />
        </div>
    );
}

export default About;